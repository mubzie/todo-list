const project = [];

class Project {
    constructor(title) {
        this.title = title;
    }
}

const project1 = new Project('new life');

project.push(project1);
console.log(project1);

console.log(task);